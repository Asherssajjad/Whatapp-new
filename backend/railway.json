{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "npx prisma db push --schema=./prisma/schema.prisma --force-reset --skip-generate; node dist/server.js"
  }
}
