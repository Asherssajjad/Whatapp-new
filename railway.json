{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push --schema=./prisma/schema.prisma --accept-data-loss --skip-generate; node dist/server.js"
  }
}
